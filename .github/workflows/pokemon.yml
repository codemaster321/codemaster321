name: Generate Pokemon

on:
  schedule:
    - cron: "0 */24 * * *"
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: ronrouzeau/github-pokemon@v1
        with:
          github_user_name: codemaster321
